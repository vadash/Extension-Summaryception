# Cache invalidation by mutation epoch

Any summary layer or snippet mutation bumps a store mutation epoch; consumers cache derived data keyed by that epoch and re-derive on drift. An event bus was rejected: SillyTavern extensions run in a single browser context where a monotonic counter gives the same staleness guarantee with no listener registration, ordering, or leak concerns.
